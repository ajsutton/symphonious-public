<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>PegaSys on Symphonious</title>
    <link>https://www.symphonious.net/categories/pegasys/</link>
    <description>Recent content in PegaSys on Symphonious</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <copyright>Adrian Sutton 2004 -</copyright>
    <lastBuildDate>Wed, 03 Jul 2019 01:04:05 +0000</lastBuildDate><atom:link href="https://www.symphonious.net/categories/pegasys/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>EIP 2159: Common Prometheus Metrics Names for Clients</title>
      <link>https://www.symphonious.net/2019/07/03/eip-2159-common-prometheus-metrics-names-for-clients/</link>
      <pubDate>Wed, 03 Jul 2019 01:04:05 +0000</pubDate>
      <guid>https://www.symphonious.net/2019/07/03/eip-2159-common-prometheus-metrics-names-for-clients/</guid>
      <description>&lt;p&gt;I&amp;rsquo;ve written up a simple little EIP to specify some standard names and meanings for Prometheus metrics that are common across Ethereum clients: &lt;a href=&#34;http://eips.ethereum.org/EIPS/eip-2159&#34;&gt;EIP-2159&lt;/a&gt;.&lt;br&gt;
It&amp;rsquo;s pretty simple and only defines four metrics but that&amp;rsquo;s enough to create quite powerful &lt;a href=&#34;https://grafana.com/dashboards/10273&#34;&gt;overview dashboards&lt;/a&gt; that would work across multiple clients.&lt;/p&gt;
&lt;p&gt; &lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Ethereum State Rent Proof of Concept</title>
      <link>https://www.symphonious.net/2019/01/14/ethereum-state-rent-proof-of-concept/</link>
      <pubDate>Sun, 13 Jan 2019 23:59:38 +0000</pubDate>
      <guid>https://www.symphonious.net/2019/01/14/ethereum-state-rent-proof-of-concept/</guid>
      <description>&lt;p&gt;I&amp;rsquo;ve had the opportunity to do some proof of concept development of the &lt;a href=&#34;https://github.com/ledgerwatch/eth_state/blob/master/State_rent.pdf&#34;&gt;Ethereum state-rent proposal&lt;/a&gt; that Alexey Akhunov has been leading on the Pantheon code base. The proposal evolved as the work continued so the actual implementation is now a lot simpler than described in that PDF.&lt;/p&gt;
&lt;p&gt;Note that the aim is to explore what it takes to implement the proposal, not to create production ready code.  The current work is all available on on &lt;a href=&#34;https://github.com/ajsutton/pantheon/tree/state-rent&#34;&gt;my state-rent branch&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;The easiest way to begin exploring the changes required to implement state-rent in Pantheon is to start with the hard forks required which translate to new ProtocolSpecs in Pantheon which are defined in &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/mainnet/MainnetProtocolSpecs.java&#34;&gt;MainnetProtocolSpecs&lt;/a&gt;. Ultimately there should be four hard forks:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Introduce replay protection for accounts that are evicted&lt;/li&gt;
&lt;li&gt;Charge fixed state rent for “owned” accounts&lt;/li&gt;
&lt;li&gt;Charge state rent for newly allocated storage&lt;/li&gt;
&lt;li&gt;Charge state rent for pre-existing storage&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;fork-1-replay-protection&#34;&gt;Fork 1: Replay Protection&lt;/h2&gt;
&lt;p&gt;When accounts are evicted, their nonce would reset to 0, potentially enabled old transactions to be replayed. There are a few ways to prevent this, my preference was to introduce &lt;a href=&#34;https://gist.github.com/holiman/5300039af83375e1698117619554acf7&#34;&gt;temporal production&lt;/a&gt; (variant 2 in the PDF) which is a simple extra validation to perform before processing transactions. As such, I skipped implementing it in the proof of concept.&lt;/p&gt;
&lt;h2 id=&#34;fork-2-state-rent-for-owned-accounts&#34;&gt;Fork 2: State Rent for Owned Accounts&lt;/h2&gt;
&lt;p&gt;In this milestone “owned” accounts are charged a fixed rent per block. An “owned” account is defined as one without code in the PoC. To avoid having to process every account on every block, the rent is only recalculated when the account is changed at the end of the block (ie a change would already have to be written to disk for it).&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; Formalise the definition of owned account more. Particularly, clarify if an account created with empty code is considered owned or not.&lt;/p&gt;
&lt;p&gt;The ProtocolSpec for this fork is defined based on Constantinople with three changes:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;&#34;&gt;&lt;code class=&#34;language-java&#34; data-lang=&#34;java&#34;&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;public&lt;/span&gt; &lt;span style=&#34;color:#66d9ef&#34;&gt;static&lt;/span&gt; ProtocolSpecBuilder&lt;span style=&#34;color:#f92672&#34;&gt;&amp;lt;&lt;/span&gt;Void&lt;span style=&#34;color:#f92672&#34;&gt;&amp;gt;&lt;/span&gt; &lt;span style=&#34;color:#a6e22e&#34;&gt;stateRentOwnedAccountsDefinition&lt;/span&gt;(
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;      &lt;span style=&#34;color:#66d9ef&#34;&gt;final&lt;/span&gt; &lt;span style=&#34;color:#66d9ef&#34;&gt;int&lt;/span&gt; chainId, &lt;span style=&#34;color:#66d9ef&#34;&gt;final&lt;/span&gt; &lt;span style=&#34;color:#66d9ef&#34;&gt;long&lt;/span&gt; rentEnabledBlockNumber) {
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;  &lt;span style=&#34;color:#66d9ef&#34;&gt;return&lt;/span&gt; constantinopleDefinition(chainId)
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;rentCost&lt;/span&gt;(Wei.&lt;span style=&#34;color:#a6e22e&#34;&gt;fromGwei&lt;/span&gt;(2))
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;rentProcessor&lt;/span&gt;(rentCost &lt;span style=&#34;color:#f92672&#34;&gt;-&amp;gt;&lt;/span&gt; &lt;span style=&#34;color:#66d9ef&#34;&gt;new&lt;/span&gt; OwnedAccountsStateRentProcessor(rentCost, rentEnabledBlockNumber))
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;accountInit&lt;/span&gt;(StateRentOwnedAccountsAccountInit::&lt;span style=&#34;color:#66d9ef&#34;&gt;new&lt;/span&gt;)
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;name&lt;/span&gt;(&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;StateRentOwnedAccounts&amp;#34;&lt;/span&gt;);
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;}
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Firstly, we set a rent cost of 2 Gwei/block. This is a pretty arbitrary figure and will likely need tweaking. This value is used by the rent processor we also set but has been split out so it&amp;rsquo;s easy for a hypothetical future fork to change the rent cost without having to implement a completely different rent processor.&lt;/p&gt;
&lt;p&gt;Finally, we set a &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/mainnet/account/StateRentOwnedAccountsAccountInit.java&#34;&gt;StateRentOwnedAccountsAccountInit&lt;/a&gt; which is used to setup defaults for any newly created accounts (post-fork). In this case, it sets the rent balance to zero and rent block to the current block number. These are two new fields added to account state in this fork. This is the first time the new account process has varied in a hard fork, so there was a bunch of plumbing code required to add the concept of a pluggable &lt;em&gt;AccountInit&lt;/em&gt; but none of it difficult.&lt;/p&gt;
&lt;p&gt;However, it&amp;rsquo;s also the first hard fork that adds fields to account state, which makes serialising and deserialising them a little more difficult. That code is in &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/worldstate/AccountSerializer.java&#34;&gt;AccountSerializer&lt;/a&gt;. Fortunately it&amp;rsquo;s easy in RLP to check if you are at the end of the current list or not, so when deserialising we can tell if rent balance and rent block have been added to this account state by checking if we&amp;rsquo;re at the end of the list (&lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/worldstate/AccountSerializer.java#L51&#34;&gt;line 51&lt;/a&gt;).&lt;/p&gt;
&lt;p&gt;Calculating the rent owed is handled by &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/mainnet/staterent/OwnedAccountsStateRentProcessor.java&#34;&gt;OwnedAccountsStateRentProcessor&lt;/a&gt; with much of the code able to be reused with later forks via &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/mainnet/staterent/AbstractRentProcessor.java&#34;&gt;AbstractRentProcessor&lt;/a&gt;. The rent balance is one of the very few things in Ethereum that can be negative which makes life a little frustrating as the existing Wei class used for account balance can&amp;rsquo;t be reused and there&amp;rsquo;s a few points where things need to be converted between Wei and BigInteger but it&amp;rsquo;s manageable.&lt;/p&gt;
&lt;p&gt;Rent is only recalculated when the account has been changed in the block. In Pantheon the simplest way to achieve this was to piggy-back on the concept of “touched” accounts used in Spurious Dragon. We simply iterate through the touched accounts and check if the root of their account state trie is dirty (which is also already tracked), if it is we recalculate rent via the RentProcessor.&lt;/p&gt;
&lt;p&gt;At this stage, any account which is unable to pay its rent can be deleted in the same way it would have been if it were empty. Again, simple to reuse existing code for this.&lt;/p&gt;
&lt;p&gt;For the record, actually calling the rent processor is hooked into &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/mainnet/MainnetBlockProcessor.java#L164&#34;&gt;MainnetBlockProcessor&lt;/a&gt; so that it happens as the last step in processing a block.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; Additional clarification is required about exactly when rent should be charged. In the PoC it&amp;rsquo;s applied even after paying the miner which seems sensible but there are a number of things happening at the end of a block so it needs to be completely clear.&lt;/p&gt;
&lt;p&gt;Note that the priority queue for eviction has been scrapped (I forget why it was originally required…).&lt;/p&gt;
&lt;h2 id=&#34;fork-3-state-rent-for-new-storage&#34;&gt;Fork 3: State Rent for New Storage&lt;/h2&gt;
&lt;p&gt;This fork introduces quite a bit of new stuff, but most importantly adds a new &lt;em&gt;storagesize&lt;/em&gt; field to account state which is adjusted as part of every SSTORE call. Existing storage is not included in the &lt;em&gt;storagesize&lt;/em&gt; field at this point, only a count of the storage size either added or cleared since fork 3 became active. The field is added when new accounts are created, when rent is recalculated for an existing account or when an SSTORE call results in the account storage size changing.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; Need to be clear about exactly when this field is first added (particularly if multiple SSTORE calls are made that leave the storage at the same size or even completely unchanged).&lt;/p&gt;
&lt;p&gt;The ProtocolSpec for this fork is based on the one for fork 2 but with a few changes:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;&#34;&gt;&lt;code class=&#34;language-java&#34; data-lang=&#34;java&#34;&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;public&lt;/span&gt; &lt;span style=&#34;color:#66d9ef&#34;&gt;static&lt;/span&gt; ProtocolSpecBuilder&lt;span style=&#34;color:#f92672&#34;&gt;&amp;lt;&lt;/span&gt;Void&lt;span style=&#34;color:#f92672&#34;&gt;&amp;gt;&lt;/span&gt; &lt;span style=&#34;color:#a6e22e&#34;&gt;stateRentNewStorageDefinition&lt;/span&gt;(
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;      &lt;span style=&#34;color:#66d9ef&#34;&gt;final&lt;/span&gt; &lt;span style=&#34;color:#66d9ef&#34;&gt;int&lt;/span&gt; chainId, &lt;span style=&#34;color:#66d9ef&#34;&gt;final&lt;/span&gt; &lt;span style=&#34;color:#66d9ef&#34;&gt;long&lt;/span&gt; rentEnabledBlockNumber) {
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;  &lt;span style=&#34;color:#66d9ef&#34;&gt;return&lt;/span&gt; stateRentOwnedAccountsDefinition(chainId, rentEnabledBlockNumber)
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;gasCalculator&lt;/span&gt;(StateRentNewStorageGasCalculator::&lt;span style=&#34;color:#66d9ef&#34;&gt;new&lt;/span&gt;)
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;evmBuilder&lt;/span&gt;(MainnetEvmRegistries::stateRentNewStorage)
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;rentCost&lt;/span&gt;(Wei.&lt;span style=&#34;color:#a6e22e&#34;&gt;fromGwei&lt;/span&gt;(1))
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;rentProcessor&lt;/span&gt;(rentCost &lt;span style=&#34;color:#f92672&#34;&gt;-&amp;gt;&lt;/span&gt; &lt;span style=&#34;color:#66d9ef&#34;&gt;new&lt;/span&gt; StorageSizeStateRentProcessor(rentCost, rentEnabledBlockNumber))
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;accountInit&lt;/span&gt;(StateRentNewStorageAccountInit::&lt;span style=&#34;color:#66d9ef&#34;&gt;new&lt;/span&gt;)
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;    .&lt;span style=&#34;color:#a6e22e&#34;&gt;name&lt;/span&gt;(&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;StateRentNewStorage&amp;#34;&lt;/span&gt;);
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;}
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;There&amp;rsquo;s a new AccountInit to set &lt;em&gt;storagesize&lt;/em&gt; to 0 when a new account is created.&lt;/p&gt;
&lt;p&gt;Rent cost is reduced to 1 Gwei since it&amp;rsquo;s now charged per byte per block. This is almost certainly still the wrong value but it definitely should be less than when rent was purely per block.&lt;/p&gt;
&lt;p&gt;We also supply a new RentProcessor, &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/mainnet/staterent/StorageSizeStateRentProcessor.java&#34;&gt;StorageSizeStateRentProcessor&lt;/a&gt;, which applies to all accounts whether they have code or not. It also charges rent based on the value of the new &lt;em&gt;storagesize&lt;/em&gt; field. If &lt;em&gt;storagesize&lt;/em&gt; hasn&amp;rsquo;t yet been set, it&amp;rsquo;s set to a fixed value to represent the size of an empty account (73 which is the length of an empty account RLP in bytes).&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; The PoC deliberately uses the value of &lt;em&gt;storagesize&lt;/em&gt; from the start of the block, ignoring any changes applied in the current block. Otherwise, if an account is untouched for 100 blocks, then increases its storage size, it will be charged rent at the increased storage size for the whole 100 blocks it was untouched. This rule may need to be tweaked slightly to use the new &lt;em&gt;storagesize&lt;/em&gt; value for the current block and the old one for the untouched blocks but it seems reasonable to defer rent changes until the block after they take effect – increased storage would have cost gas in the current block and reduced storage doesn&amp;rsquo;t really give a benefit until the block is complete and committed anyway.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;NOTE:&lt;/strong&gt; &lt;em&gt;storagesize&lt;/em&gt; may be negative during this fork, indicating that the account now uses less storage than it did when this fork first happened. After fork 5, &lt;em&gt;storagesize&lt;/em&gt; should only ever be positive.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; Rent calculation during this fork should probably use max(&lt;em&gt;storagesize&lt;/em&gt;, 0) to avoid the rent due being negative.&lt;/p&gt;
&lt;p&gt;Accounts with code are not deleted when they can&amp;rsquo;t pay their rent, they are instead “evicted” and replaced with a hash stub so they can be restored via the new RESTORETO operation (see below). This is one of the more complicated changes to work through the code – firstly just in terms of tracking evicted accounts through the transaction commit/rollback code but also in terms of detecting them in the account state trie.&lt;/p&gt;
&lt;h3 id=&#34;hash-stubs&#34;&gt;Hash Stubs&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Challenging bit:&lt;/strong&gt; Determining if an address in the account state trie is an actual account state or just a hash stub.&lt;/p&gt;
&lt;p&gt;Looking again at &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/worldstate/AccountSerializer.java&#34;&gt;AccountSerializer&lt;/a&gt; in the &lt;em&gt;serializeHashStub&lt;/em&gt; method, we can see that a hash stub is an RLP list containing the code hash and storage root hash. RLP doesn&amp;rsquo;t provide any typing information though so to detect a hash stub, we check if the first item is a nonce or a hash. If it is a hash stub, the size of the RLP item must be 32 bytes. Theoretically a nonce is an arbitrary unsigned scalar number, so it could wind up being 32 bytes (a 256bit number) but at least in Pantheon we already don&amp;rsquo;t support nonces that big and you&amp;rsquo;d have to spend a &lt;em&gt;lot&lt;/em&gt; of eth on gas to get a nonce to make a nonce that big. So the item length appears to be a suitable way to distinguish between account state and hash stubs.&lt;/p&gt;
&lt;p&gt;UPDATE: A smarter way to do this would have been to just count the number of items in the RLP list which is relatively straight-forward. If there are 2 items, it must be a hash stub.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; Clarify interactions for any operation that looks up an account state if the account state has been replaced by a hash stub. Currently Pantheon treats hash stubs as if they don&amp;rsquo;t exist except in the RESTORETO operation. This is mostly right, except that contract creation should consider it an address conflict if a hash stub is at the target address (not currently implemented but pretty straight forward).&lt;/p&gt;
&lt;h3 id=&#34;new-evm-operations&#34;&gt;New EVM Operations&lt;/h3&gt;
&lt;p&gt;There are three EVM operations added in this fork: &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/vm/operations/PayRentOperation.java&#34;&gt;PAYRENT&lt;/a&gt;, &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/vm/operations/RentBalanceOperation.java&#34;&gt;RENTBALANCE&lt;/a&gt;, &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/vm/operations/SSizeOperation.java&#34;&gt;SSIZE&lt;/a&gt; and &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/vm/operations/RestoreToOperation.java&#34;&gt;RESTORETO&lt;/a&gt;. Additionally &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/vm/operations/SStoreOperation.java&#34;&gt;SSTORE&lt;/a&gt; is updated to update the &lt;em&gt;storagesize&lt;/em&gt; field. PAYRENT, RENTBALANCE and the SSTORE changes are pretty trivial but RESTORETO has quite a few complexities.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; Decide on the gas cost for these new opcodes.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Tool Support:&lt;/strong&gt; RENTBALANCE returns an Int256 (signed) which I think is the only time an EVM operation returns a signed number. Need to double check compatibility for this with things like Solidity but should be fine.&lt;/p&gt;
&lt;h3 id=&#34;restore-to-operation&#34;&gt;Restore To Operation&lt;/h3&gt;
&lt;p&gt;The RESTORETO operation turns out to be one of the more interesting parts of this spec to implement. The basic workflow is slightly nuanced and involves three accounts. Let&amp;rsquo;s say that Account A is evicted, leaving behind a stub with its code and storage root hashes. To restore Account A:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Account B is created with the same code as Account A.&lt;/li&gt;
&lt;li&gt;Account C is created with code that sets up the storage in Account C to precisely match the storage Account A had before being evicted. It may do this in it&amp;rsquo;s constructor or expose functions that can be called over time to build up the correct state from multiple sources (potentially sharing the gas cost of rebuilding that state across multiple people).&lt;/li&gt;
&lt;li&gt;Account C calls RESTORETO &lt;address of Account A&gt; &lt;address of Account B&gt;. The EVM checks that account B&amp;rsquo;s code hash matches what Account A&amp;rsquo;s stub records, and that Account C&amp;rsquo;s storage root hash matches what Account A&amp;rsquo;s stub records. If so, it creates a new contract at Address A, replacing the hash stub, with the code from Account B and the storage from Account C. It also transfers any balance from Account C to Account A and destroys Account C (just as SELFDESTRUCT would).&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Mostly this is fairly straight-forward to implement as seen in &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/vm/operations/RestoreToOperation.java&#34;&gt;RestoreToOperation.java&lt;/a&gt;. The hidden catch is that most Ethereum clients, including Pantheon, don&amp;rsquo;t update the storage trie until the transaction actually commits – the changes made mid-transaction are stored in a simple HashMap which is much faster. As such, getting the storage root hash for Account C isn&amp;rsquo;t straight-forward. In fact, in the PoC &lt;a href=&#34;https://github.com/ajsutton/pantheon/blob/state-rent/ethereum/core/src/main/java/tech/pegasys/pantheon/ethereum/core/AbstractWorldUpdater.java#L433&#34;&gt;I haven&amp;rsquo;t implemented it at all&lt;/a&gt;. It&amp;rsquo;s certainly possible to create a copy of the unmodified Trie, update that with pending changes and calculate the storage root but it&amp;rsquo;s an expensive operation and that code would &lt;em&gt;only&lt;/em&gt; be exercised by RESTORETO, significantly increasing the testing required to fully cover RESTORETO.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; Find a way to change RESTORETO so that the storage root is more easily available when required.&lt;/p&gt;
&lt;p&gt;One option here would be to have execution immediately halt when RESTORETO is called, and then to perform the actual restore at the end of the transaction, much like how SELFDESTRUCT only applies at the end of the transaction. Geth and Pantheon currently update the storage trie after every transaction (they don&amp;rsquo;t necessarily calculate the root hash but that&amp;rsquo;s simple once you have the trie), but TurboGeth delays updating the trie until the end of the block, improving performance if the same account is used from multiple transactions in one block. Implementing that improvement for Pantheon is also planned. Delaying application of RESTORETO until the end of the block feels a bit too weird though.&lt;/p&gt;
&lt;p&gt;More thought is required in this area to find a solution that both makes sense to users and can be implemented without significantly increasing the surface area required for testing.&lt;/p&gt;
&lt;h2 id=&#34;fork-4-state-rent-for-existing-storage&#34;&gt;Fork 4: State Rent for Existing Storage&lt;/h2&gt;
&lt;p&gt;The PoC doesn&amp;rsquo;t include any of this fork currently, but it&amp;rsquo;s fairly straight-forward. When an account&amp;rsquo;s &lt;em&gt;rentblock&lt;/em&gt; field is first updated to a block after fork 4 is in effect, the account&amp;rsquo;s &lt;em&gt;storagesize&lt;/em&gt; field is increased by the size of its storage immediately prior to fork 3 coming into effect. Basically, we add the historical state size in.&lt;/p&gt;
&lt;p&gt;This is done as a separate fork because it gives times for client developers to precompute the storage size for every account on the various public networks at the fork 3 block and include that in a client update. This means the cost of calculating that existing storage size is done offline and doesn&amp;rsquo;t impact on performance of every node.&lt;/p&gt;
&lt;p&gt;However, because the &lt;em&gt;storagesize&lt;/em&gt; field is only updated when the account is first touched anyway, it is still feasible to calculate the existing storage size on the fly, either because the precomputed values aren&amp;rsquo;t available for that particular network or in order to verify them.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TODO:&lt;/strong&gt; It&amp;rsquo;s not explicitly stated anywhere at the moment, but when calculating the rent due for the first time after fork 4, rent should be calculated up to fork 4 block using the new-storage-only-value, then add in the existing storage size and calculate rent from fork 4 block up to the current block. Otherwise accounts start being charge rent for existing storage at different times which is too hard to understand (and unfair).&lt;/p&gt;
&lt;h2 id=&#34;other-work-required&#34;&gt;Other Work Required&lt;/h2&gt;
&lt;p&gt;There&amp;rsquo;s some other stuff that would need to be done to make state-rent production ready:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Additional JSON-RPC methods such as &lt;em&gt;getRentBalance&lt;/em&gt; and probably something to calculate the rent owing for a given account at a specific block.&lt;/li&gt;
&lt;li&gt;CALLFEE opcode. The spec refers to this to provide a way for contracts to require a rent contribution when users call into it, but there weren&amp;rsquo;t enough details to actually implement it. The main question is where the extra fee is taken from (it can&amp;rsquo;t be an extra gas charge because rent is tracked in ETH not gas).&lt;/li&gt;
&lt;li&gt;Currently if you deploy a contract with Remix it doesn&amp;rsquo;t get any eth balance so it is immediately evicted when you interact with it. This is quite confusing and frustrating as a user. We&amp;rsquo;ll either need to update tools to allow sending an initial rent balance, provide some free rent on contract creation or allow a grace period after contract creation before rent is charged.&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>Introducing Pantheon</title>
      <link>https://www.symphonious.net/2018/11/01/introducing-pantheon/</link>
      <pubDate>Thu, 01 Nov 2018 08:56:41 +0000</pubDate>
      <guid>https://www.symphonious.net/2018/11/01/introducing-pantheon/</guid>
      <description>&lt;p&gt;This week, the work I&amp;rsquo;ve been doing for the past 6 months, and that PegaSys has been working on for the past 18 months or so was released into the world. Specifically we&amp;rsquo;ve released &lt;a href=&#34;https://pegasys.tech&#34;&gt;Pantheon 0.8.1&lt;/a&gt;, our new MainNet compatible, Apache 2 licensed, Java-based Ethereum client. And &lt;a href=&#34;https://github.com/PegaSysEng/pantheon&#34;&gt;it&amp;rsquo;s open source&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;I&amp;rsquo;m pretty excited about it on a few fronts. Firstly I think it&amp;rsquo;s a pretty important thing for the Ethereum community. To be a healthy ecosystem, Ethereum needs to have diversity in its clients to avoid a bug in one client taking out or accidentally hard forking the entire network. Currently though, Geth and Parity dominate the Ethereum client landscape. Pantheon clearly won&amp;rsquo;t change that in the short term, but it is backed by significant engineering resources to help it keep up with the ever changing Ethereum landscape and be a dependable option.&lt;/p&gt;
&lt;p&gt;I&amp;rsquo;m also really excited that Pantheon is released under the Apache 2.0 license. Both Parity and Geth along with most other clients are licensed under the GPL or LGPL. There are still a large number of enterprises that completely avoid the GPL and LGPL which has closed off Ethereum to them. Having Pantheon available under a permissive license and in a highly familiar language like Java will make it much easier for many enterprises to start using, building on and innovating with Ethereum.&lt;/p&gt;
&lt;p&gt;Pantheon will also be building out functionality from the Enterprise Ethereum standard for things like privacy and permissioning to make private chains more powerful and flexible. Meanwhile we have a significant number of researches continuing to work on developing new ways to get the most out of Ethereum.&lt;/p&gt;
&lt;p&gt;Finally I&amp;rsquo;m quite excited to be able to contribute to an open source project as my full time job. I&amp;rsquo;ve had the opportunity to do some open source for work in the past but only on a fairly small scale. It&amp;rsquo;s a bit daunting to have everything in the open and on the record, but I&amp;rsquo;m really looking forward to engaging with the community and being able to show exactly what I&amp;rsquo;ve been doing.&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>