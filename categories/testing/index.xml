<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Testing on Symphonious</title>
    <link>https://www.symphonious.net/categories/testing/</link>
    <description>Recent content in Testing on Symphonious</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <copyright>Adrian Sutton 2004 -</copyright>
    <lastBuildDate>Fri, 07 Jul 2017 21:44:58 +0000</lastBuildDate><atom:link href="https://www.symphonious.net/categories/testing/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Moolah Diaries – Multi-tenant Support for Testing</title>
      <link>https://www.symphonious.net/2017/07/08/moolah-diaries-multi-tenant-support-for-testing/</link>
      <pubDate>Fri, 07 Jul 2017 21:44:58 +0000</pubDate>
      <guid>https://www.symphonious.net/2017/07/08/moolah-diaries-multi-tenant-support-for-testing/</guid>
      <description>&lt;p&gt;Experience at LMAX has very clearly demonstrated the benefits of good &lt;a href=&#34;https://www.symphonious.net/2014/04/12/testinglmax-test-isolation/&#34;&gt;test isolation&lt;/a&gt;, so one of the first things I added to Moolah was multi-tenant support. Each account is a completely isolated little world which is perfect for testing.&lt;/p&gt;
&lt;p&gt;Given that I&amp;rsquo;ve completely punted on storing passwords by delegating authentication to Google (and potentially other places in the future since it&amp;rsquo;s using &lt;a href=&#34;https://github.com/hapijs/bell/&#34;&gt;bell&lt;/a&gt; to handle third party authentication), there&amp;rsquo;s actually no user creation step at all which makes it even easier. As a result it&amp;rsquo;s not just acceptance tests that are benefiting from the isolation but also the database tests which no longer need to delete everything in the database to give themselves a clear workspace.&lt;/p&gt;
&lt;p&gt;It&amp;rsquo;s all quite civilised really.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Testing@LMAX – Isolate UI Tests with vncserver</title>
      <link>https://www.symphonious.net/2016/10/25/testinglmax-isolate-ui-tests-with-vncserver/</link>
      <pubDate>Tue, 25 Oct 2016 06:41:46 +0000</pubDate>
      <guid>https://www.symphonious.net/2016/10/25/testinglmax-isolate-ui-tests-with-vncserver/</guid>
      <description>&lt;p&gt;One reason that automated UI tests can be unreliable is that they tend to be sensitive to what else is on screen at the time and even things like the current screen size. Developers running the tests locally also find it annoying to have windows opening and closing on their machine while the test runs and are unable to do anything else because their clicking might interfere with the test.&lt;/p&gt;
&lt;p&gt;At LMAX we solve that by isolating tests in their own X session, created using vncserver. We simply start vncserver with:&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;vncserver :20 -geometry 1600x1200
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;Then set DISPLAY=:20 as an environment variable when starting WebDriver&amp;rsquo;s Firefox instance:&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;FirefoxBinary firefoxBinary &lt;span style=&#34;color: #666666;&#34;&gt;=&lt;/span&gt; &lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;new&lt;/span&gt; FirefoxBinary&lt;span style=&#34;color: #666666;&#34;&gt;();&lt;/span&gt;
firefoxBinary&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;setEnvironmentProperty&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;DISPLAY&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; &lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;:20&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;The Firefox window then pops up in it&amp;rsquo;s own isolated X session. We can still use a vnc client to watch as the test runs but we can also let it run in the background and continue using the machine for other things. In CI it allows us to run UI tests on a headless server.&lt;/p&gt;
&lt;p&gt;Since we run a number of tests in parallel, in CI we start a number of vncserver instances and allocate a different one to each running test to ensure they&amp;rsquo;re completely isolated.&lt;/p&gt;
&lt;p&gt;Simple, but incredibly effective.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Testing@LMAX – Screenshots with Selenium/WebDriver</title>
      <link>https://www.symphonious.net/2016/10/25/testinglmax-screenshots-with-selenium/</link>
      <pubDate>Tue, 25 Oct 2016 06:23:18 +0000</pubDate>
      <guid>https://www.symphonious.net/2016/10/25/testinglmax-screenshots-with-selenium/</guid>
      <description>&lt;p&gt;When an automated UI test fails, it can be hard to tell exactly what went wrong just from the failure message. The failure message typically just says that some element the test was looking for wasn&amp;rsquo;t found, but it doesn&amp;rsquo;t tell you what was there.  Was there an error message displayed instead? Was the operation still executing? Did something completely unexpected happen instead?&lt;/p&gt;
&lt;p&gt;To answer those questions our DSL automatically captures a screenshot when any UI operation fails and we include a link to it in the failure message. That way when someone reviews the test result they can see exactly what was on screen which typically makes it straight forward to identify what went wrong and fix it.&lt;/p&gt;
&lt;p&gt;Until recently we&amp;rsquo;d been using the convenient and helpful looking &lt;code&gt;TakesScreenshot.getScreenshotAs&lt;/code&gt; method that WebDriver provides.  For example:&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;&lt;span style=&#34;color: #666666;&#34;&gt;((&lt;/span&gt;TakesScreenshot&lt;span style=&#34;color: #666666;&#34;&gt;)&lt;/span&gt;webDriver&lt;span style=&#34;color: #666666;&#34;&gt;).&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;getScreenshotAs&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;br /&gt;&lt;/span&gt;&lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;    new&lt;/span&gt; SaveScreenshotOutputType&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;pngFilename&lt;span style=&#34;color: #666666;&#34;&gt;));&lt;/span&gt;
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;As expected, this creates a PNG image in the specified location that looks for all the world like a screenshot of the browser content. Unfortunately, it&amp;rsquo;s lying.&lt;/p&gt;
&lt;p&gt;WebDriver actually does something very clever and gets the browser to render the page content into a canvas element and then saves that as the PNG file. This is an extremely close approximation of what the page looks like with two important exceptions:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;It doesn&amp;rsquo;t respect the viewport size so body content is never scrolled off-screen.&lt;/li&gt;
&lt;li&gt;Any browser chrome or random other windows that have popped up aren&amp;rsquo;t shown.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Both of these things can be an issue – the scrolled-off-screen one being the most problematic.  Modern WebDriver quite accurately simulates a user clicking and typing keys so if somethings not on screen it can&amp;rsquo;t be clicked. When your test fails because an element was “present but not visible” and the screenshot shows it as very clearly visible, hilarity ensues. Very frustrating hilarity.&lt;/p&gt;
&lt;p&gt;To fix this we&amp;rsquo;ve started taking honest-to-goodness screenshots. Since all our tests get their own X session (courtesy of vncserver) their windows are completely isolated from each other and a dump of the entire screen will capture precisely what a real user would see, browser chrome and scrolling included. Linux provides an entertaining array of options for capturing screenshots from the command line but the one that happened to be already installed was import, part of the ImageMagick suite. We simply execute:&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;import -display :20 -window root screenshot.png
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;where :20 is the X display this particular test has been allocated and screenshot.png is where we want the screenshot to wind up.&lt;/p&gt;
&lt;p&gt;Since the WebDriver screenshot can be useful as well – for example finding out an error message is displayed at the top of the screen  we continue to grab that too.&lt;/p&gt;
&lt;p&gt;Finally, for completeness we grab a dump of the DOM to a HTML file so we can later inspect what IDs, classes, attributes etc are present, including any hidden elements. &lt;code&gt;webDriver.getPageSource()&lt;/code&gt; makes that easy and we append an extra HTML comment that includes webDriver.getCurrentUrl() for good measure.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Testing@LMAX – Compatibility Tests</title>
      <link>https://www.symphonious.net/2016/03/13/testinglmax-compatibility-tests/</link>
      <pubDate>Sat, 12 Mar 2016 20:00:09 +0000</pubDate>
      <guid>https://www.symphonious.net/2016/03/13/testinglmax-compatibility-tests/</guid>
      <description>&lt;p&gt;Once an application goes live, it is absolutely essential that any future changes are able to be work with the existing data in production, typically by migrating it as changes are required. That existing data and the migrations applied to it are often the riskiest and least tested functions in the system. Mistakes in a migration will at best cause a multi-hour outage while backups are restored and more likely will subtly corrupt data, producing incorrect results that may go unnoticed for long periods making it impossible to roll back. At LMAX we reduce that risk by running compatibility tests.&lt;/p&gt;
&lt;h3 id=&#34;sanitised-data&#34;&gt;Sanitised Data&lt;/h3&gt;
&lt;p&gt;The first requirement for testing data migrations is testing data that as production-like as possible. It&amp;rsquo;s almost never a good idea to bring actual production data into development and it&amp;rsquo;s definitely never going to happen for a finance company like LMAX. Instead, we have a sanitiser that runs in production and generates a very heavily sanitised version of production data that still has the same “shape”. For example, it has the same number of accounts, the same distribution of open positions across instruments, null values in the same places there&amp;rsquo;s null values in production and so on. However it replaces anything that&amp;rsquo;s even vaguely personally identifiable or sensitive information. If in doubt, sanitise it out.&lt;/p&gt;
&lt;p&gt;Despite being heavily sanitised, we still treat it as data that should be secured but it can be brought into our development, testing and staging environments.&lt;/p&gt;
&lt;p&gt;We have multiple production environments so we have sanitised data that matches the shape of each of those environments.&lt;/p&gt;
&lt;h3 id=&#34;can-migrations-run&#34;&gt;Can Migrations Run?&lt;/h3&gt;
&lt;p&gt;Once we have sanitised data the most basic check is to confirm the migration process will actually complete successfully. This ensures we can release successfully but doesn&amp;rsquo;t give us any real confidence that the migration is successful. For such a primitive check it&amp;rsquo;s surprisingly effective as it picks up the common errors of changing columns to NOT NULL when the production data actually does have null values, or adding a unique constraint to tables when the content isn&amp;rsquo;t actually unique.&lt;/p&gt;
&lt;h3 id=&#34;did-migrations-work&#34;&gt;Did Migrations Work?&lt;/h3&gt;
&lt;p&gt;The obvious next step is to write tests to confirm that migrations actually worked. Our compatibility test jobs are setup so that we can easily write a JUnit test that will be run after migrations complete so we can verify the state. &lt;/p&gt;
&lt;p&gt;The most direct form of test is an example based test. We select some examples from the production dataset and write assertions to check that specific bit of the data migrated in the way we expect. The down side is that we&amp;rsquo;re dealing with live production data which is regularly updated so it&amp;rsquo;s possible that our examples will change after we&amp;rsquo;ve written the test and then, correctly, migrate to something different to what we expect. Still, these are often useful to put through for a single run as a sanity test when developing the migration, then delete them.&lt;/p&gt;
&lt;p&gt;Slightly more generic, we can write a test that assert constraints that must be true immediately after the migration completes. For example, when we made permissions more fine-grained we needed to assign a new type of payment role to every account that used real money, but not to any demo accounts (which use pretend money). We can write a test to verify that migration worked correctly quite easily, however once the migration goes out admin users may add or remove the role to different accounts and the constraint would no longer hold. For cases like that we simply delete the test once the migration has gone live at which point it&amp;rsquo;s done its job anyway.  We also mark the test with a @ValidUntil annotation that makes it clear that the test has a limited life time in case we forget to delete it.&lt;/p&gt;
&lt;p&gt;Finally, we can often identify constraints that should always be valid and write permanent tests for them. These are extremely powerful, testing not just that our current migration works correctly but that no future migration breaks that expectation.&lt;/p&gt;
&lt;h3 id=&#34;did-something-unexpected-happen&#34;&gt;Did Something Unexpected Happen?&lt;/h3&gt;
&lt;p&gt;The compatibility tests that should always hold true have an additional benefit – they give us early feedback that the production data has diverged from expectations for some reason, typically because a bug slipped through. We can then investigate, fix the bug to prevent any more data issues and work out how to clean up the problem.&lt;/p&gt;
&lt;p&gt;Obviously, finding issues only after production data has gone wrong is not something we ever want to do but it&amp;rsquo;s still a useful safety net if something slips through all our pre-release testing and the database schema constraints we use. Typically when we do find issues they are minor inconsistencies that don&amp;rsquo;t cause any issues now, but are like little time bombs just waiting for a future release to assume it can&amp;rsquo;t possibly happen. So even getting feedback that late in the process often allows us to avoid any user-noticeable effects.&lt;/p&gt;
&lt;h3 id=&#34;making-them-easy&#34;&gt;Making Them Easy&lt;/h3&gt;
&lt;p&gt;We have a base class we extend our compatibility tests from which makes it easy to get a connection to the database and has a few handy utilities for asserting things. By far the most useful however is the &lt;code&gt;assertNoRowsMatch&lt;/code&gt; method. It does exactly what it says – takes an SQL query and asserts that no rows match. If any do, it prints them out so you get really useful debug information to start investigating the problem. For example:&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;&lt;span style=&#34;color: #aa22ff;&#34;&gt;@Test&lt;/span&gt;
&lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;public&lt;/span&gt; &lt;span style=&#34;color: #b00040;&#34;&gt;void&lt;/span&gt; &lt;span style=&#34;color: #0000ff;&#34;&gt;shouldHaveAPrincipalForEveryAccount&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;()&lt;/span&gt; &lt;span style=&#34;color: #666666;&#34;&gt;{&lt;/span&gt;
  assertNoRowsMatch&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;
    &lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;SELECT a.account_id, a.name &#34;&lt;/span&gt; &lt;span style=&#34;color: #666666;&#34;&gt;+&lt;/span&gt;
    &lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;FROM account a &#34;&lt;/span&gt; &lt;span style=&#34;color: #666666;&#34;&gt;+&lt;/span&gt;
    &lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;LEFT JOIN principal p ON a.principal_id = p.principal_id &#34;&lt;/span&gt; &lt;span style=&#34;color: #666666;&#34;&gt;+&lt;/span&gt;
    &lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;WHERE p.principal_id IS NULL&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
&lt;span style=&#34;color: #666666;&#34;&gt;}&lt;/span&gt;
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;If we&amp;rsquo;ve somehow wound up with an account with no principal that could log into it the test will print the account ID and name so we can investigate what happened and clean up the data.&lt;/p&gt;
&lt;p&gt; &lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Testing@LMAX – Making Test Output Useful</title>
      <link>https://www.symphonious.net/2016/02/29/testinglmax-making-test-output-useful/</link>
      <pubDate>Sun, 28 Feb 2016 23:14:18 +0000</pubDate>
      <guid>https://www.symphonious.net/2016/02/29/testinglmax-making-test-output-useful/</guid>
      <description>&lt;p&gt;Just like production code, you should assume things are going to go wrong in your tests and when it does you want good logging to help track down what happened and why. So just like production code, you should use a logging framework within your DSL, use meaningful log levels and think about what info you&amp;rsquo;d need in the logs if something went wrong (and what you don&amp;rsquo;t). There&amp;rsquo;s also a few things we&amp;rsquo;ve found very useful specifically in our test logging.&lt;/p&gt;
&lt;h3 id=&#34;log-alias-to-real-name-mappings&#34;&gt;Log Alias to Real Name Mappings&lt;/h3&gt;
&lt;p&gt;Since &lt;a href=&#34;https://www.symphonious.net/2015/06/08/testinglmax-aliases/&#34;&gt;the DSL uses aliases&lt;/a&gt;, if we want to poke around in the exchange manually to understand why a test failed, we need to know the real names to use. So whenever we create a real name for an alias we log some information about it. For example when creating an instrument:&lt;/p&gt;
&lt;pre&gt;21:28:53,916 WARN  …rdersWithSuppliedInstructionIds [AdminAPI] Created instrument &#39;instrument&#39; (actual name: &#39;instrument-54369ih64k63&#39;, externalId: 180423, internalId: 180422) on tradeReportingGroup 1003&lt;/pre&gt;
&lt;p&gt;All the key information we need is in that log statement – the alias, real name plus unique identifiers (externalId and internalId).&lt;/p&gt;
&lt;h3 id=&#34;name-your-threads&#34;&gt;Name Your Threads&lt;/h3&gt;
&lt;p&gt;We use a custom JUnit test runner (via the @RunWith annotation) so we can run tests within a test suite in parallel. With tests running in parallel all their output gets mixed up and becomes hard to read.  Recently we started setting the test thread names to the name of the test case. &lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;&lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;private&lt;/span&gt; &lt;span style=&#34;color: #b00040;&#34;&gt;void&lt;/span&gt; &lt;span style=&#34;color: #0000ff;&#34;&gt;executeTest&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;
  &lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;final&lt;/span&gt; FrameworkMethod method&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; 
  &lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;final&lt;/span&gt; Description description&lt;span style=&#34;color: #666666;&#34;&gt;)&lt;/span&gt; &lt;span style=&#34;color: #666666;&#34;&gt;{&lt;/span&gt;
    Thread&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;currentThread&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;().&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;setName&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;
      getThreadName&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;description&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;getMethodName&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;()));&lt;/span&gt;
    methodBlock&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;method&lt;span style=&#34;color: #666666;&#34;&gt;).&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;evaluate&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;();&lt;/span&gt;
&lt;span style=&#34;color: #666666;&#34;&gt;}&lt;/span&gt;
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;We actually trim the method name to 30 characters (cutting off the &lt;em&gt;start&lt;/em&gt; rather than the end which tends to work better with the way we name tests) so we get output like:&lt;/p&gt;
&lt;pre&gt;21:28:47,356 INFO  …tityAndPriceAndNoStopLossOffset [TestContext] Created PartyCode XMCS (alias: marketMaker)&lt;br /&gt;&lt;br /&gt;21:28:47,356 INFO  …erWithZeroSuppliedInstructionId [TestContext] Created PartyCode U6HK (alias: marketMaker)&lt;br /&gt;&lt;br /&gt;21:28:47,356 INFO  …ctionIdIfFirstOrderHasCompleted [TestContext] Created PartyCode 3DS6 (alias: marketMaker)&lt;br /&gt;&lt;br /&gt;21:28:47,356 INFO  …StopLossOffsetAndAStopLossPrice [TestContext] Created PartyCode XFY8 (alias: marketMaker)&lt;/pre&gt;
&lt;p&gt;There are a few cases where we spawn additional threads within a test (typically to pull data from long poll or other push event channels). In those cases we generally pass the thread name down with an additional prefix (e.g. LongPoll-…StopLossOffsetAndAStopLossPrice) so we can still associate that output with the right test.&lt;/p&gt;
&lt;h3 id=&#34;time-traveller-names&#34;&gt;Time Traveller Names&lt;/h3&gt;
&lt;p&gt;The way &lt;a href=&#34;https://www.symphonious.net/2014/04/01/testinglmax-time-travel-and-the-tardis/&#34;&gt;we allow time travelling tests to run in parallel&lt;/a&gt; is reasonably complex – only one thread ever actually executes the time travel and there&amp;rsquo;s a bunch of cross-thread coordination – so our thread names aren&amp;rsquo;t as useful in that little area of code. As such we give each test we&amp;rsquo;re currently running a time traveller name so we get log output from the Tardis like:&lt;/p&gt;
&lt;pre&gt;The Doctor asking to travel to mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;br /&gt;Clara asking to travel to mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;br /&gt;Captain Jack asking to travel to mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;br /&gt;Rory asking to travel to mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;br /&gt;Missy asking to travel to mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;br /&gt;Amy asking to travel to mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;br /&gt;River asking to travel to mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;br /&gt;Rose asking to travel to mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;br /&gt;Time travelling to: mondayOpen (Mon Mar 14 07:00:00 UTC 2016)&lt;/pre&gt;
&lt;p&gt;We may have gotten a little carried away with the Doctor Who theme but having a name for each time traveller makes it far easier to understand what each test is waiting for.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Alert Dialogs Do Not Appear When Using WebDriverBackedSeleniu</title>
      <link>https://www.symphonious.net/2015/12/08/alert-dialogs-do-not-appear-when-using-webdriverbackedseleniu/</link>
      <pubDate>Tue, 08 Dec 2015 03:18:28 +0000</pubDate>
      <guid>https://www.symphonious.net/2015/12/08/alert-dialogs-do-not-appear-when-using-webdriverbackedseleniu/</guid>
      <description>&lt;p&gt;With Selenium 1, JavaScript alert and confirmation dialogs were intercepted by the Selenium JavaScript library so they never appeared on-screen and were accessed using selenium.isAlertPresent(), selenium.isConfirmationPresent(), selenium.chooseOkOnNextConfirmation() and similar methods.&lt;/p&gt;
&lt;p&gt;With Selenium 2 aka WebDriver, the dialogs &lt;em&gt;do&lt;/em&gt; appear on screen and you access them with webDriver.switchTo().alert() which returns an Alert instance for further interactions.&lt;/p&gt;
&lt;p&gt;However, when you use WebDriverBackedSelenium to mix Selenium 1 and WebDriver APIs – for example, during migrations from one to the other – the alerts don&amp;rsquo;t appear on screen and webDriver.switchTo().alert() always throws a NoAlertPresentException. This makes migration problematic because selenium.chooseOkOnNextConfirmation() doesn&amp;rsquo;t have any effect if the next confirmation is triggered by an action performed directly through Web Driver.&lt;/p&gt;
&lt;p&gt;The solution is to avoid WebDriverBackedSelenium entirely and instead create a WebDriverCommandProcessor, configure it to not emulate dialogs and pass it to DefaultSelenium.&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;&lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;public&lt;/span&gt; Selenium &lt;span style=&#34;color: #0000ff;&#34;&gt;createSelenium&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;
  &lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;final&lt;/span&gt; WebDriver webDriver&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; 
  &lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;final&lt;/span&gt; String baseUrl&lt;span style=&#34;color: #666666;&#34;&gt;)&lt;/span&gt; 
&lt;span style=&#34;color: #666666;&#34;&gt;{&lt;/span&gt;
  WebDriverCommandProcessor commandProcessor &lt;span style=&#34;color: #666666;&#34;&gt;=&lt;/span&gt; 
    &lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;new&lt;/span&gt; &lt;span style=&#34;color: #0000ff;&#34;&gt;WebDriverCommandProcessor&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;baseUrl&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; &lt;span style=&#34;color: #666666;&#34;&gt;()&lt;/span&gt; &lt;span style=&#34;color: #666666;&#34;&gt;-&amp;gt;&lt;/span&gt; webDriver&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
  commandProcessor&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;setEnableAlertOverrides&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;false&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
  processor&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;start&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;();&lt;/span&gt;
  &lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;return&lt;/span&gt; &lt;span style=&#34;color: #008000; font-weight: bold;&#34;&gt;new&lt;/span&gt; &lt;span style=&#34;color: #0000ff;&#34;&gt;DefaultSelenium&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;commandProcessor&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
&lt;span style=&#34;color: #666666;&#34;&gt;}&lt;/span&gt;
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;The key method call here is setEnableAlertOverride(false) which disables the alert emulation.  Note that the Selenium 1 methods for interacting with dialogs will no longer work – you have to switch all alert handling over to WebDriver.&lt;/p&gt;
&lt;p&gt;Also note the subtle but important use of a closure to provide a Supplier&lt;WebDriver&gt; instead of just passing the webDriver. The overloaded constructor instance that takes a WebDriver immediately calls start() on the processor and setEnableAlertOverrides must be called before start().&lt;/p&gt;
&lt;p&gt;It&amp;rsquo;s unfortunate, but understandable, that Selenium 1 and WebDriver style alert interactions can&amp;rsquo;t be mixed as that would make migration a lot easier. At least with this approach you can convert your alert interactions over in one batch then convert all the other interactions incrementally.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Testing@LMAX – Introducing ElementSpecification</title>
      <link>https://www.symphonious.net/2015/12/04/testinglmax-introducing-elementspecification/</link>
      <pubDate>Fri, 04 Dec 2015 03:26:30 +0000</pubDate>
      <guid>https://www.symphonious.net/2015/12/04/testinglmax-introducing-elementspecification/</guid>
      <description>&lt;p&gt;Today LMAX Exchange has released &lt;a href=&#34;https://github.com/LMAX-Exchange/elementspec&#34;&gt;ElementSpecification&lt;/a&gt;, a very small library we built to make working with selectors in selenium/WebDriver tests easier. It has three main aims:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Make it easier to understand selectors by using a very English-like syntax&lt;/li&gt;
&lt;li&gt;Avoid common pitfalls when writing selectors that lead to either brittle or intermittent tests&lt;/li&gt;
&lt;li&gt;Strongly discourage writing overly complicated selectors.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Essentially, we use ElementSpecification anywhere that we would have written CSS or XPath selectors by hand. ElementSpecification will automatically select the most appropriate format to use – choosing between a simple ID selector, CSS or XPath.&lt;/p&gt;
&lt;p&gt;Making selectors easier to understand doesn&amp;rsquo;t mean making locators shorter – CSS is already a very terse language. We actually want to use more characters to express our intent so that future developers can read the specification without having to decode CSS. For example, the CSS:&lt;/p&gt;
&lt;pre&gt;#data-table tr[data-id=&#39;78&#39;] .name&lt;/pre&gt;
&lt;p&gt;becomes:&lt;/p&gt;
&lt;pre&gt;anElementWithId(&#34;data-table&#34;)&lt;br /&gt;    .thatContainsA(&#34;tr&#34;).withAttributeValue(&#34;data-id&#34;, &#34;78&#34;)&lt;br /&gt;    .thatContainsAnElementWithClass(&#34;name&#34;)&lt;/pre&gt;
&lt;p&gt;Much longer, but if you were to read the CSS selector to yourself, it would come out a lot like the ElementSpecification syntax. That allows you to stay focussed on what the test is doing instead of pausing to decode the CSS. It&amp;rsquo;s also reduces the likelihood of misreading a vital character and misunderstanding the selector.&lt;/p&gt;
&lt;p&gt;With ElementSpecification essentially acting as an adapter layer between the test author and the actual CSS, it&amp;rsquo;s also able to avoid some common intermittency pitfalls. In fact, the reason ElementSpecification was first built was because really smart people kept attempting to locate an element with a classname using:&lt;/p&gt;
&lt;pre&gt;//*[contains(@class, &#39;valid&#39;)]&lt;/pre&gt;
&lt;p&gt;which looks ok, but incorrectly also matches an element with the class ‘invalid&amp;rsquo;. Requiring the class attribute to exactly match ‘valid&amp;rsquo; is too brittle because it will fail if an additional class is added to the element. Instead, ElementSpecification would generate:&lt;/p&gt;
&lt;pre&gt;contains(concat(&#39; &#39;, @class, &#39; &#39;), &#39; valid &#39;)&lt;/pre&gt;
&lt;p&gt;which is decidedly unpleasant to have to write by hand.&lt;/p&gt;
&lt;p&gt;The biggest benefit we&amp;rsquo;ve seen from ElementSpecification though is that fact that it has a deliberately limited set of abilities. You can only descend down the DOM tree, never back up and never across to siblings. That makes selectors &lt;em&gt;far&lt;/em&gt; easier to understand and avoids a lot of unintended coupling between the tests and incidental properties of the DOM. Sometimes it means augmenting the DOM to make it more semantic – things like adding a “data-id” attribute to rows as in the example above. It&amp;rsquo;s surprisingly rare how often we need to do that and surprising how useful those extra semantics wind up being for a whole variety of reasons anyway.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Travis CI</title>
      <link>https://www.symphonious.net/2015/10/01/travis-ci/</link>
      <pubDate>Thu, 01 Oct 2015 01:26:18 +0000</pubDate>
      <guid>https://www.symphonious.net/2015/10/01/travis-ci/</guid>
      <description>&lt;p&gt;Probably the best thing I&amp;rsquo;ve discovered with my recent playing is &lt;a href=&#34;https://travis-ci.org&#34;&gt;Travis CI&lt;/a&gt;. I&amp;rsquo;ve known about it for quite some time, even played with it for simple projects but never with anything with any real complexity. Given this project uses rails which wants a database for pretty much every test and that database has to be postgres because I&amp;rsquo;m using it&amp;rsquo;s jsonb support, plus capybara and phantomjs for good measure, this certainly isn&amp;rsquo;t a simple project to test.&lt;/p&gt;
&lt;p&gt;Despite that, getting things running on Travis CI was quick and easy. The container based builds give you a well controlled environment out of the box and they already have a heap of software you&amp;rsquo;re likely to need ready to go. Other things can be easily provided using a simple .travis.yml file.&lt;/p&gt;
&lt;p&gt;While it&amp;rsquo;s common to have a build server these days, it&amp;rsquo;s quite uncommon for them to be so well controlled and able to be rebuilt quickly and easily – most wind up being special little snowflakes in some way or other. So building on Travis CI is immediately a massive leap forward in testing practices.&lt;/p&gt;
&lt;p&gt;The two things we do at LMAX that I can&amp;rsquo;t see how to do with Travis CI (yet) is creating &lt;a href=&#34;https://www.symphonious.net/2014/03/25/testinglmax-test-results-database/&#34;&gt;a database of historic test results&lt;/a&gt; that can be easily queried (at the individual test level, not the build result level), and &lt;a href=&#34;https://www.symphonious.net/2014/03/30/testinglmax-distributed-builds-with-romero/&#34;&gt;running a build in parallel with the tests automatically load balanced across a number of hosts&lt;/a&gt;. I wouldn&amp;rsquo;t expect either to be provided as part of the free service anyway but they seem like the next major areas of value it could offer (with significant effort required I imagine).&lt;/p&gt;
&lt;p&gt;Still, it&amp;rsquo;s been a real luxury to have a full CI setup for hacking on a little side project. So thanks for the generosity Travis CI.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Testing@LMAX – Replacements in DSL</title>
      <link>https://www.symphonious.net/2015/08/27/testinglmax-replacements-in-dsl/</link>
      <pubDate>Thu, 27 Aug 2015 10:29:33 +0000</pubDate>
      <guid>https://www.symphonious.net/2015/08/27/testinglmax-replacements-in-dsl/</guid>
      <description>&lt;p&gt;Given our DSL makes heavy use of &lt;a href=&#34;https://www.symphonious.net/2015/06/08/testinglmax-aliases/&#34;&gt;aliases&lt;/a&gt;, we often have to provide a way to include the real name or ID as part of some string. For example, an audit record for a new account might be:&lt;/p&gt;
&lt;pre&gt;Created account 127322 with username someUser123.&lt;/pre&gt;
&lt;p&gt;But in our acceptance test we&amp;rsquo;d create the user with:&lt;/p&gt;
&lt;pre&gt;registrationAPI.createUser(&#34;someUser&#34;);&lt;/pre&gt;
&lt;p&gt;someUser is just an alias, the DSL creates a unique username to use and the system assigns a unique account ID that the DSL keeps track of for us. So to write a test that the new account audit record is written correctly, we need a way to build the expected audit string.&lt;/p&gt;
&lt;p&gt;Our initial attempt was straight forward enough:&lt;/p&gt;
&lt;pre&gt;adminAPI.verifyAuditEntry(&lt;br /&gt;  &#34;expression: Created account &amp;lt;accountId&amp;gt; with username &amp;lt;username&amp;gt;.&#34;,&lt;br /&gt; &#34;account: someUser&#34;,&lt;br /&gt;);&lt;/pre&gt;
&lt;p&gt;The DSL substitutes &lt;accountId&gt; and &lt;username&gt; with the real values for us. Simple, neat, worked well for quite a while. Except that over time we kept finding more and more things that needed to be substituted and encountered situations where an audit log would reference to two different accounts or multiple instruments, leading us to have &lt;accountId2&gt; and &lt;accountId3&gt; replacements.&lt;/p&gt;
&lt;p&gt;So a little while back some of my clever colleagues changed things around to a slightly more complex but much more flexible syntax:&lt;/p&gt;
&lt;pre&gt;adminAPI.verifyAuditEntry(&lt;br /&gt;  &#34;expression: Created account &amp;lt;accountId:someUser&amp;gt; with username &amp;lt;username:someUser&amp;gt;.&#34;&lt;br /&gt;);&lt;/pre&gt;
&lt;p&gt;Essentially, the replacement now contains both the type of value to insert as well as the alias to look up. This has been a minor revolution for our DSL – it&amp;rsquo;s now much easier to handle all sorts of complex cases and it&amp;rsquo;s much clearer which alias is being used for each replacement.&lt;/p&gt;
&lt;p&gt;The biggest win though, is that because all the required information to perform the expansions is inside the one string – not requiring any extra DSL parameters – the replacement algorithm can be easily shared across all the different places in the DSL that need to support replacements. New types of things to be replaced can easily be added in one place and are then available everyone as well.&lt;/p&gt;
&lt;p&gt;It&amp;rsquo;s surprising how much benefit you can get from such a simple little change.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Testing@LMAX – Abstraction by DSL</title>
      <link>https://www.symphonious.net/2015/06/05/testinglmax-abstraction-by-dsl/</link>
      <pubDate>Fri, 05 Jun 2015 06:34:08 +0000</pubDate>
      <guid>https://www.symphonious.net/2015/06/05/testinglmax-abstraction-by-dsl/</guid>
      <description>&lt;p&gt;At LMAX, all our acceptance tests are written using a high level DSL. This gives us two key advantages:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Tests can focus on what they&amp;rsquo;re testing with the details and mechanics hidden behind the DSL&lt;/li&gt;
&lt;li&gt;When things change we can update the DSL implementation to match, avoiding the need to change each test that happens to touch on the area.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The DSL that LMAX uses is probably not what most people think of when hearing the term DSL – it doesn&amp;rsquo;t attempt to read like plain English, just simplifies things down significantly. We&amp;rsquo;ve actually open sourced the simple little library that is the entrance-way to what we think of as the DSL – creatively named &lt;a href=&#34;https://github.com/LMAX-Exchange/Simple-DSL&#34;&gt;simple-dsl&lt;/a&gt;. It&amp;rsquo;s essentially the glue between what we write in an acceptance test and the plain-java implementation behind that.&lt;/p&gt;
&lt;p&gt;As a simple example here&amp;rsquo;s a test that creates a user, instrument and places an order:&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;registrationAPI&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;createUser&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;user&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
adminAPI&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;marketOperations&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;createInstrument&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;instrument&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
&lt;p&gt;tradingAPI&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;login&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;&amp;ldquo;user&amp;rdquo;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
tradingAPI&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;placeOrder&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;&amp;ldquo;instrument&amp;rdquo;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; &lt;span style=&#34;color: #ba2121;&#34;&gt;&amp;ldquo;quantity: 5&amp;rdquo;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; &lt;span style=&#34;color: #ba2121;&#34;&gt;&amp;ldquo;type: market&amp;rdquo;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;br /&gt;&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;    &amp;ldquo;expectedStatus: UNMATCHED&amp;rdquo;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
&lt;/pre&gt;&lt;/p&gt;
&lt;/div&gt;
&lt;p&gt;Overall, the aim is to have the acceptance tests written at a very high level – focussing on &lt;em&gt;what&lt;/em&gt; should happen but leaving the &lt;em&gt;how&lt;/em&gt; to the DSL implementation. The tradingAPI.placeOrder call is a good example of this, it&amp;rsquo;s testing that when the user places an order on an instrument with no liquidity, it won&amp;rsquo;t be matched. In the DSL that&amp;rsquo;s actually a two step process, first place the order and receive a synchronous OK response to say the order was received, then when the order reaches the matching engine an asynchronous event will be emitted to say the order was not matched. We could have made that two separate calls in the acceptance test but that would have exposed too much detail about &lt;em&gt;how&lt;/em&gt; the system works when what we really care about is that the order is unfilled, how that&amp;rsquo;s reported is an implementation detail.&lt;/p&gt;
&lt;p&gt;However that does mean that the implementation of the DSL an important part of the specification of the system. The acceptance tests express the user requirements and the DSL expresses the technical details of those requirements.&lt;/p&gt;
&lt;h3 id=&#34;model-the-types-of-users-and-interactions&#34;&gt;Model the Types of Users and Interactions&lt;/h3&gt;
&lt;p&gt;All our acceptance tests extends a base class, DslTestCase, that exposes a number of public variables that act as the entry points to the system (registrationAPI, adminAPI and tradingAPI in the example above). Each of these roughly represent a way that certain types of users interact with the system. So registrationAPI works with the API exposed by our registration gateway – the same APIs that our sign-up process on the website talks to.  adminAPI uses the same APIs our admin console talks to and tradingAPI is the API that both our UI uses and that many of our clients interact with directly.&lt;/p&gt;
&lt;p&gt;We also have UI variants like adminUI and tradingUI that use selenium to open a browser and test the UI as well.&lt;/p&gt;
&lt;p&gt;Our system tends to have a strong correlation between the type of user and the entry point to the system they use so our DSL mostly maps to the gateways into our system, but in other systems it may be more appropriate to focus more on the type of user regardless of what their entry point into the system is. Again the focus should be on &lt;em&gt;what&lt;/em&gt; happens more than &lt;em&gt;how&lt;/em&gt;. The way you categorise functions in the DSL should aid you in thinking that way.&lt;/p&gt;
&lt;p&gt;That said, our top level DSL concepts aren&amp;rsquo;t entirely restricted to just the system entry point they model. For example the registrationAPI.createUser call in the example will initially talk to the system&amp;rsquo;s registration API, but since a new account isn&amp;rsquo;t very useful until it deposits funds, it then talks to the admin console to approve the registration and credit some funds into the users account. There&amp;rsquo;s a large dose of pragmatism involved in the DSL implementation with the goal being to make it easy to read and write the acceptance tests themselves and we&amp;rsquo;re willing to sacrifice a little design purity to get that (but only a little).&lt;/p&gt;
&lt;p&gt;Top level concepts often further categorise the functionality they provide, for example our admin console that adminAPI drives has a lot of functionality and is used by a range of types of users, so it sub-categorises into things like marketOperations, customerServices, risk, etc.&lt;/p&gt;
&lt;h3 id=&#34;add-reusable-components-to-the-dsl&#34;&gt;Add Reusable Components to the DSL&lt;/h3&gt;
&lt;p&gt;One of the signs that people don&amp;rsquo;t understand the design of our DSL is when they extract repetitive pieces of tests into a private method within the test itself. On the surface this seems  like a reasonable idea, allowing that sequence of actions to be reused by multiple tests in the file. If the sequence is useful in many test cases within one file and significant enough to be worth the indirection of extracting a method it&amp;rsquo;s almost inevitably useful across many files.&lt;/p&gt;
&lt;p&gt;Instead of extracting a private method, put reusable pieces into the DSL itself. Then they&amp;rsquo;ll be available to all your tests.  More importantly though, you can make that method fit into the DSL style properly – in our case, using simple-dsl to pass parameters instead of a fixed set of method parameters. &lt;/p&gt;
&lt;p&gt;One of our top level concepts in the DSL is ‘workflows&amp;rsquo;. It bundles together broader sequences of actions that cut across the boundaries of any one entrance point. It&amp;rsquo;s a handy home for many of the reusable functions we split out. The down side is it&amp;rsquo;s currently a real grab bag of random stuff and could do with some meaningful sub-categorisation. Naming is hard…&lt;/p&gt;
&lt;h3 id=&#34;design-to-avoid-intermittency&#34;&gt;Design to Avoid Intermittency&lt;/h3&gt;
&lt;p&gt;The way the DSL is designed is a key weapon in the fight against intermittency. The first rule is to design each function to appear synchronous as much as possible. The LMAX Exchange is a highly asynchronous system design but our DSL hides that as much as possible.&lt;/p&gt;
&lt;p&gt;The most useful pattern for this is that whenever you provide a setter-type function it should automatically wait and verify that the effect has been fully applied by checking the equivalent getter-type API. So the end of the DSL implementation for registrationAPI.createUser is a waiter that polls our broker service waiting for the account to actually show up there with the initial balance we credited. That way the test can carry on and place an order immediately without intermittently being rejected for a lack of funds.&lt;/p&gt;
&lt;p&gt;The second key pattern applies when verifying values. We produce a lot of reports as CSV files so originally had DSL like:&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;adminAPI&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;finance&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;downloadCashflowReport&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;date: today&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; &lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;rememberAs: myCsvFile&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
adminAPI&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;finance&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;verifyCashflowReportContains&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;csvFile: myCsvFile&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; &lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;amount: 10.00&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;Apart from being pretty horrible to read, this leads to a lot of intermittency because our system doesn&amp;rsquo;t guarantee that cash flows will be recorded to the database immediately, it&amp;rsquo;s done asynchronously so is only guaranteed to happen within a reasonable time. Instead it&amp;rsquo;s much better to write:&lt;/p&gt;
&lt;div style=&#34;background: #f8f8f8; overflow: auto; width: auto; padding: .2em .6em;&#34;&gt;
  &lt;pre style=&#34;margin: 0; line-height: 125%;&#34;&gt;adminAPI&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;finance&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color: #7d9029;&#34;&gt;verifyCashflowReportContains&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;date: today&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;,&lt;/span&gt; &lt;span style=&#34;color: #ba2121;&#34;&gt;&#34;amount: 10.00&#34;&lt;/span&gt;&lt;span style=&#34;color: #666666;&#34;&gt;);&lt;/span&gt;
&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;Then inside the DSL you can use a waiter to poll the cashflow CSV until it does contain the expected value or whatever you define as a reasonable time elapses and the test times out and fails. Again, having the test focus on &lt;em&gt;what&lt;/em&gt; and the DSL dealing with &lt;em&gt;how&lt;/em&gt; allows us to write better tests.&lt;/p&gt;
&lt;h3 id=&#34;dont-get-too-fancy-with-the-dsl&#34;&gt;Don&amp;rsquo;t Get Too Fancy with the DSL&lt;/h3&gt;
&lt;p&gt;The first thought most people have when they see our DSL is that it could be so much better if we used static types and chained method calls to get the compiler to validate more stuff and have refactoring tools work well. It sounds like a great idea, our simple string based DSL seems far too primitive to work in practice but we&amp;rsquo;ve actually tried it the other way as well and it&amp;rsquo;s not as great as it sounds.&lt;/p&gt;
&lt;p&gt;Inevitably when you try to make the DSL too much like English or try to get the compiler more involved you add quite a lot of complexity to the DSL implementation which makes it a lot harder to maintain so the cost of your acceptance tests goes up – exactly the opposite of what you were intending.&lt;/p&gt;
&lt;p&gt;The trade offs will vary considerably depending on which language you&amp;rsquo;re using for your tests and the best style of DSL to create will vary significantly. I strongly suspect though that regardless of language the best DSL is a radically simple one, just that different things are radically simple in different languages.&lt;/p&gt;
&lt;h3 id=&#34;dsls-matter&#34;&gt;DSL&amp;rsquo;s Matter&lt;/h3&gt;
&lt;p&gt;This was meant to be a quick article before getting on to what I really wanted to talk about but suddenly I&amp;rsquo;m 1500 words in and still haven&amp;rsquo;t discussed anything about the implementation side of the DSL.&lt;/p&gt;
&lt;p&gt;It turns out that while our DSL might be simple and something we take for granted, it&amp;rsquo;s a huge part of what makes our acceptance tests easily maintainable instead of gradually becoming a huge time sink that prevents any change to the system. My intuition is that those people who have tried acceptance tests and found them too expensive to maintain have failed to find the right style of abstraction in the DSL they use, leaving their tests too focused on &lt;em&gt;how&lt;/em&gt; instead of &lt;em&gt;what&lt;/em&gt;.&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>